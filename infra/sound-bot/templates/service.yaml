apiVersion: v1
kind: Service
metadata:
  name: {{ include "sound-bot.fullname" . }}
  labels:
    {{- include "sound-bot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
  selector:
    {{- include "sound-bot.selectorLabels" . | nindent 4 }}
