apiVersion: v1
kind: Service
metadata:
  name: {{ include "lessgames.fullname" . }}
  labels:
    {{- include "lessgames.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
  selector:
    {{- include "lessgames.selectorLabels" . | nindent 4 }}
