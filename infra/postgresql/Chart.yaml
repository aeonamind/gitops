apiVersion: v2
name: postgresql
description: PostgreSQL database with Vault-managed credentials
type: application
version: 0.1.0
appVersion: "18.4.0"
dependencies:
  - name: postgresql
    version: "18.8.0"
    # Bitnami classic Helm index redirects to OCI; use OCI directly for ArgoCD pulls.
    repository: oci://registry-1.docker.io/bitnamicharts
